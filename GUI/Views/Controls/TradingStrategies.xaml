<UserControl x:Class="GUI.Views.Controls.TradingStrategies"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:StrategiesInTradeViewModel/>
    </UserControl.DataContext>
    
    <DataGrid ItemsSource="{Binding Strategies}" SelectedItem="{Binding SelectedStrategy}">
        <DataGrid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Stop"/>
                <MenuItem Header="Show Info"/>
            </ContextMenu>
        </DataGrid.ContextMenu>
        <DataGrid.Columns>
            <DataGridTextColumn Header="Instrument" 
                                Binding="{Binding Instrument.FullName}"/>
            <DataGridTextColumn Header="Account"
                                Binding="{Binding MainSettings.Account}"/>
            <DataGridTextColumn Header="PnL"
                                Binding="{Binding Pnl}"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
